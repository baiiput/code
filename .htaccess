# ===========================================
# WAREHOUSE MANAGEMENT SECURITY
# Apache 2.2 Compatible Version
# ===========================================

# Enable URL rewriting
RewriteEngine On
RewriteBase /

# Exclude /reports/ and /assets/ from all rewrite rules
RewriteRule ^(reports|assets)/ - [L]

# Remove .php extension from URL
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.php -f
RewriteRule ^(.*)$ $1.php [L]

# Redirect .php URLs to non-.php URLs
RewriteCond %{THE_REQUEST} ^GET\ /[^?\s]+\.php
RewriteRule ^(.*)\.php$ /$1 [R=301,L]

# Protect .htaccess
<Files .htaccess>
    Order allow,deny
    Deny from all
</Files>

# Protect sensitive files
<FilesMatch "\.(env|ini|log|sh|sql|bak|config)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect warehouse.sql
<Files warehouse.sql>
    Order allow,deny
    Deny from all
</Files>

# Protect reset SQL files
<FilesMatch "reset.*\.sql$">
    Order allow,deny
    Deny from all
</FilesMatch>
